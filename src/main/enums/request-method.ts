export enum RequestMethod {
    // eslint-disable-next-line no-unused-vars
    GET = 'get',
    // eslint-disable-next-line no-unused-vars
    POST = 'post',
    // eslint-disable-next-line no-unused-vars
    PATCH = 'patch',
    // eslint-disable-next-line no-unused-vars
    DELETE = 'delete',
}
